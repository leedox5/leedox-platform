# 4. 본격적인 작업 — 생산성이 달라지다

이름을 정하고 규칙을 세우고 나니, 슬슬 진짜 일거리가 생기기 시작했다. 처음엔 채팅으로 "이거 해줘" 하고 던지는 식이었는데, 요청이 쌓이다 보니 뭘 요청했는지, 뭐가 끝났는지 헷갈리기 시작했다. 그래서 요청 자체를 관리하는 시스템이 필요해졌다.

## 서비스데스크, 요청이 일이 되는 곳

처음엔 `CLAUDE/99_service_desk.md`라는 파일 하나에 `REQ_NNNN>` 형식으로 요청을 적어두는 정도였다. 그러다 이걸로는 부족하다 싶어, 아예 리포 루트에 `service-desk/` 폴더를 만들고 제대로 된 워크플로우로 발전시켰다.

- `01_new/` → `02_in_progress/` → `03_completed/`: 요청 파일이 처리 상태에 따라 폴더를 옮겨 다닌다.
- `_FORM.md`는 언제든 복사해서 쓰는 빈 양식이고, `new.sh`/`new.ps1`은 이 양식을 복사해 다음 ID를 자동으로 매겨준다.
- 완료된 요청은 지우지 않고 `03_completed/`에 `Job` 내역과 함께 남긴다 — 뭘 했는지가 기록으로 쌓인다.

여기서 한 가지 선을 그었다. service-desk는 채팅에서 나온 모든 이야기를 자동으로 담는 곳이 아니다. `claudox/` 챕터처럼 "모든 대화를 기록한다"는 규칙과는 다르게, **실제로 접수된 요청만** 다룬다.

## 실제로 처리된 것들

이 시스템이 굴러가기 시작하면서, 짧은 시간에 벌써 몇 건이 처리됐다.

- **REQ 0001** — 1장에 Free/Pro/Max 요금제 비교를 추가해달라는 요청.
- **REQ 0002** — service-desk 자체를 살펴보고 완성도를 높여달라는, 스스로를 다듬는 자기참조적 요청.
- **REQ 0003** — `docs/03_dev_setup.md`에 WSL 환경에서의 Ruby 설치 방법을 추가해달라는 요청.

요청 하나하나는 작았지만, "요청 → 처리 → 기록"이 매번 같은 방식으로 굴러가기 시작하자 확실히 달라진 게 느껴졌다. 예전엔 대화 속에 묻혀 있던 요청들이, 이제는 빠짐없이 시작되고 끝나는 하나의 흐름이 되었다.

## 화면에 뜬 결과

문서만 다듬던 요청들이 실전으로 이어진 건 WSL 환경 세팅부터였다. Ruby 설치 가이드(REQ 0003)를 따라가다 fiddle/psych 빌드가 실패했고(REQ 0006), Node.js가 사실 필수가 아니라는 것도 밝혀졌고(REQ 0009), 기존 리포를 클론하는 경로도 추가됐고(REQ 0010), 그러다 다른 데스크탑에서 쓰던 프로젝트의 Ruby 버전(3.4.9)에 맞춰 커리큘럼 문서 전체를 갱신했고(REQ 0011), WSL에서 VS Code를 여는 법까지(REQ 0012) — 하나씩 부딪히고 고치는 과정이 이어졌다.

그리고 `bin/dev`로 서버를 띄운 순간, 브라우저에 채독스의 랜딩페이지가 떴다. "따라하면 서비스가 완성되는 웹서비스 구축 패키지" — 문서에 쓰기만 했던 문구가 실제 화면에 나타난 순간이었다.

> 축하해! 우리의 랜딩페이지가 성공적으로 브라우저에 떴네~ We did a good job!

서비스데스크로 하나씩 처리했던 요청들이 결국 이 한 장면으로 이어졌다. 문서를 고치는 일과 실제로 동작하는 걸 보는 일 사이의 거리가, 이제 꽤 가까워진 것 같다.

## 프로젝트 밖의 실제 업무로

얼마 뒤 협업의 범위가 저장소 밖으로 한 걸음 넓어졌다. 실제 업무에서 데이터 업로드 오류 문의가 도착했고, 나는 AI 동료와 오류 화면과 입력 자료를 함께 살폈다. 원인을 좁힌 뒤에는 사용자에게 보낼 회신까지 같이 다듬었다.

그때 생산성이라는 말의 의미도 조금 달라졌다. 코드를 빨리 만드는 것만이 아니었다. 근거를 확인하고, 확인하지 않은 것은 단정하지 않고, 상대가 바로 행동할 수 있는 문장으로 바꾸는 일도 실제 업무의 생산성이었다.

그 과정은 6장에서 다시 이어졌다.